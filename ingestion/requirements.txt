# SDK Snowflake Streaming Ingest (Snowpipe Streaming, architecture haute-performance)
# Versions epinglees = builds reproductibles (une release cassante ne pete pas le build en silence).
snowpipe-streaming==1.5.0
# Client WebSocket pour les flux Binance
websocket-client==1.9.0
